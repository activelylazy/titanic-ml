
# Titanic Machine Learning

This repo is a small example of using logistic regression to predict survivors from the titanic.

# Getting started

* run `npm i` to download the dependencies
* run `npm start` to start the application
* visit [http://localhost:3000](http://localhost:3000/) in a browser
* open `public/titanic.csv` to view the test data
